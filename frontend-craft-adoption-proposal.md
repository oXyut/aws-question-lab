# frontend-craft 導入に関する進言書

## 1. 結論

`frontend-craft` は、本プロジェクトに導入する価値がある。

ただし、これはUIコンポーネントやテーマを追加して画面を自動的に洗練するライブラリではない。AIコーディングエージェントに対して、UI設計・デザイントークン・アクセシビリティ・レスポンシブ対応・視覚QA・リファクタリングの判断基準を与えるための開発支援ツールである。

したがって、目的は「導入後に画面が一気におしゃれになること」ではなく、今後のAI実装で画面が汎用的なカードUIやテンプレート的なレイアウトへ戻ることを防ぎ、プロダクト固有の視覚言語を継続的に維持することである。

導入判断の目安は **7/10**。まずはCodex向け・プロジェクトローカル・限定的なスキルだけで試すことを推奨する。

## 2. 導入を検討する背景

現在のフロントエンドは、機能面・レスポンシブ対応・視覚QAの水準は低くない。一方、UI全体には以下のような「AIが生成した業務UI」に見えやすい要素が残っている。

- 入力画面が「大見出し + ステップ表示 + 大きなカード + 機能紹介」という汎用的な構成になっている
- 薄い境界線、小さな角丸、淡い背景、カードの積み重ねが多い
- AWS Question Lab固有の強みである「問題文・要件・構成図・根拠の連動」が、視覚的な主役になりきっていない
- ヘッダー、入力画面、図解画面で、似ているが少し異なる色・角丸・タイポグラフィの定義が存在する
- AI生成の失敗や処理状態など、重要な状態表示が画面上で大きく主張し、主タスクとの優先順位が崩れる場面がある

一方で、図解ワークスペースには既に以下の良い土台がある。

- 72pxの濃紺ヘッダーとナビゲーションレール
- 問題文、要件、構成図、選択肢評価を同一画面で扱う構成
- AWS公式アイコンを使った実データ由来の図解
- 要件選択、選択肢切り替え、図の操作、根拠表示の連動
- モバイル、キーボード、生成進捗、エラー状態まで含めた既存の視覚QA

つまり、全面的な作り直しが必要なのではなく、既存の良い構造を壊さず、視覚言語と設計判断を統合することが課題である。

## 3. 現在の実装から確認できる課題

### 3.1 デザイントークンが分散している

`styles.css`、`studio.css`、`shell.css` にそれぞれ色・背景・境界線・角丸が定義されている。

- [`src/styles.css`](./src/styles.css) に `--navy`、`--orange`、`--border`、`--muted`、`--radius` がある
- [`src/studio.css`](./src/studio.css) に別値の同名トークンがある
- [`src/shell.css`](./src/shell.css) ではヘッダーやレールの色が直接指定されている
- `#fff`、淡いグレー、境界線色などのハードコードが多数存在する

この状態では、エージェントに画面修正を依頼するたびに、既存の値を再発明したり、別の色を追加したりしやすい。`frontend-craft` のデザインシステム／トークン系のルールを使う価値が最も高い部分である。

### 3.2 ページ単位の視覚的な役割がまだ弱い

入力画面は、問題を貼り付けて処理を始めるための作業画面であるにもかかわらず、機能紹介やステップ説明が比較的大きく配置されている。図解画面は逆にプロダクト固有性が強いが、薄い枠線とカードの連続により、一般的な管理画面にも見える。

画面ごとに、以下のような役割を明示する必要がある。

- 入力画面: 最短で問題を投入し、処理状態と保存範囲を理解できる画面
- 確認画面: AIの読み取り結果を人が修正・確認する画面
- 図解画面: 問題文から判断根拠までをたどる学習ワークスペース
- エラー状態: 入力や既存データを失わず、次の復旧操作がすぐ分かる画面

### 3.3 品質ルールが既存文書に留まっている

[`design-qa.md`](./design-qa.md) には、視覚比較、レスポンシブ、キーボード、状態確認についてかなり丁寧な記録がある。しかし、今後のエージェントが毎回その内容を設計判断へ反映する仕組みとは分離している。

`frontend-craft` を導入する場合は、既存のQA知見を置き換えるのではなく、デザインルール・状態マトリクス・検証コマンドとしてAIの作業フローに接続するべきである。

## 4. frontend-craftから吸収する候補

### 優先度A: まず導入するもの

#### `fec-ui-design`

プロダクトの目的、利用者、情報密度、視覚的なアンカー、状態、レスポンシブ、視覚QAを先に決めてから実装するためのスキル。

本プロジェクトでは、AWSアイコンやデータフローを「装飾」ではなく、学習体験の中心となる視覚的アンカーとして扱う判断に使う。

#### `fec-design-token-mapper`

散在している色・余白・角丸・文字サイズを、既存コードを壊さずに統合するためのスキル。

まず以下を一元化する。

- プロダクトカラー
- 状態色: 成功、比較上不利、違反、要確認、処理中、失敗
- 背景・境界線・本文・補助文の階層
- 角丸、影、余白、表示密度
- 本文と操作部品の文字サイズ

#### `fec-ui-checker`

実装後の視覚的な違和感、テンプレート感、階層崩れ、状態不足をレビューするためのスキル。

既存の `design-qa.md` と統合し、画面のスクリーンショット比較を継続する。

### 優先度B: 次に導入するもの

- `fec-responsive-layout`: 図解キャンバス、評価パネル、固定フッターの狭い画面での振る舞いを標準化する
- `fec-accessibility-check`: キーボード操作、フォーカス、タッチ領域、読み上げ名称を確認する
- `fec-motion-interaction`: 生成中、選択中、図の再生、開閉状態のモーションを必要最小限に整理する
- `fec-code-review`: UI変更時に型、状態、レンダリング、スタイル、アクセシビリティをまとめてレビューする

### 優先度C: 現時点では保留するもの

- Figmaなどのデザインツール連携
- 全エージェントの常時有効化
- セッション終了時に必ず全テスト・全ビルドを実行するフック
- Tailwind向けのデザインシステム
- 本プロジェクトに存在しないフレームワーク向けルール

本プロジェクトはReact + TypeScript + Viteで、CSSを直接管理している。導入のためにTailwindや別のUIフレームワークへ移行する必要はない。

## 5. 推奨する導入方法

### 5.1 まずローカルスコープで導入する

グローバル設定ではなく、対象プロジェクトだけに接続する。

```sh
cd /Users/suzukiyuto/work/aws-dea/aws-question-lab
npx @bovinphang/frontend-craft install --local codex
```

実行前に、生成されるファイル、設定、フックの内容を確認する。グローバル導入は、複数プロジェクトで同じルールを使う必要性が確認できてからでよい。

### 5.2 最初の作業はUIの作り直しではなく、設計メモの作成にする

いきなりCSSを書き換えず、以下を先に文書化する。

1. 対象利用者: AWS DEAを学習する個人
2. 中心タスク: 問題の条件から、選択肢の比較理由と構成図を理解する
3. 信頼の根拠: 原文、AWS公式資料、出典確認、既知の正解との区別
4. 視覚的アンカー: 要件から構成図・選択肢評価へつながるデータフロー
5. デザインの方向: 学習用ワークベンチ、読みやすい高密度、装飾は控えめ
6. 避けるもの: 汎用的なヒーロー、意味のないカード、過剰なグラデーション、同じ構造の反復

### 5.3 変更は垂直スライスで進める

最初の対象は入力画面だけに限定する。

- 既存の入力機能・画像入力・任意項目・保存処理は維持
- 見出し、説明、ステップ表示、入力カード、エラー表示の優先順位を整理
- トークンを一元化
- 375px / 768px / 1440pxで確認
- `npm run typecheck`、`npm test`、`npm run build`、必要に応じてE2Eを実行

入力画面で効果が確認できた後、図解ワークスペースへ広げる。

## 6. 避けるべき導入パターン

- `frontend-craft` の生成物を無検証で既存の `AGENTS.md` に上書きする
- 既存のAWSアイコン、図解、視覚QAを捨てて、汎用的な新デザインへ置き換える
- AIが提案した新しい色・フォント・アニメーションを、プロダクト目的の検討なしに採用する
- 全スキル、全エージェント、全フックを一度に有効化する
- lintスクリプトが存在しない状態で、lintを前提とする自動フックを有効化する
- 「AIっぽさ」を消すためだけに、不要な装飾・イラスト・グラデーション・アニメーションを追加する
- 既存のユーザー導線や生成状態を、視覚改善の名目で変更する

`frontend-craft` 自身も、参照システムをそのままコピーせず、対象プロジェクト固有の設計へ吸収する方針を掲げている。この方針に従い、AWS Question Labの既存資産を中心に改善する。

## 7. 受け入れ基準

### 視覚

- 色、余白、角丸、文字サイズ、状態色が一元化されている
- 入力画面の最初の画面で、問題投入という主タスクが明確である
- 図解画面で、問題文・要件・構成図・評価の関係が視覚的に理解できる
- 同じ役割のUIが画面ごとに別の見た目になっていない
- 連続するカードや説明ブロックが、内容上の理由なく繰り返されていない

### 機能・状態

- 入力中、空、生成中、完了、失敗、再試行、無効、選択中、フォーカス中を確認できる
- 失敗時に、既存入力と履歴が失われず、次の操作が分かる
- 図解の選択、要件の切り替え、評価の切り替えが既存どおり動作する
- オフラインHTML出力の見た目と機能を壊していない

### アクセシビリティ・レスポンシブ

- キーボードだけで主要導線を操作できる
- フォーカスリングが視認できる
- 小さい操作部品でも十分なタッチ領域がある
- 375px、768px、1440pxで横スクロールや重要情報の欠落がない
- `prefers-reduced-motion` を尊重する

### 検証

```sh
npm run typecheck
npm test
npm run build
npm run test:e2e
```

## 8. リスクと対策

### ルールの過剰適用

多くのスキルを有効にすると、簡単な修正にも大きな設計・レビュー手順が適用され、開発速度が落ちる可能性がある。

対策: 最初はUI設計、トークン、視覚QAに限定する。

### 既存ルールとの衝突

プロジェクト固有の `AGENTS.md`、既存の `design-qa.md`、テスト手順と、生成されたルールが重複・矛盾する可能性がある。

対策: 既存のプロジェクトルールを正とし、重複するルールは一つに統合する。生成物を無検証で追加しない。

### 自動フックによる開発体験の悪化

保存時の整形やセッション終了時の全検証が、現在のスクリプト構成と一致しない可能性がある。

対策: フックは個別に内容を確認し、必要なものだけ有効にする。特にlint前提の処理は、lint導入方針を決めてから扱う。

### 外部パッケージへの依存

CLI、ルール、フック、MCP設定を外部パッケージに依存するため、更新時に挙動が変わる可能性がある。

対策: バージョンを確認し、導入差分をGitで記録する。更新前後に型チェック、テスト、ビルド、視覚QAを実行する。

## 9. 本流エージェントへの依頼事項

本書をもとに、本流エージェントには次の順で作業してもらいたい。

1. `frontend-craft` のCodex向けローカル導入内容をdry-runまたは生成物確認で調査する
2. 既存の `AGENTS.md`、`design-qa.md`、npm scriptsとの重複・衝突を確認する
3. `fec-ui-design`、`fec-design-token-mapper`、`fec-ui-checker` 相当のルールだけを採用する案を作る
4. 入力画面を対象に、既存機能を変えない小さな視覚改善を実施する
5. 視覚比較と既存テストを実行し、導入前後の差分を報告する
6. 効果が確認できた場合のみ、図解ワークスペースとレスポンシブ対応へ拡張する

## 10. 最終判断

導入は推奨する。ただし、採用すべきものは「frontend-craftの全体」ではなく、本プロジェクトに必要な設計判断の部分である。

最初の成功条件は、画面を派手にすることではない。入力画面と図解画面の役割が明確になり、色・余白・状態・コンポーネントの判断が一貫し、次回以降のAI実装でも同じ品質を再現できることである。

