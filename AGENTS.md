# 開発ルール

- 日本語で回答する。
- 問題文・アップロード画像・生成履歴・認証情報をGitに追加しない。実行時データは `.local/` に保存する。
- 実装後は差分、次のステップ、完了率、問題点、ユーザーに必要な手順を報告する。
- 変更はGitで記録し、テストとビルドを確認してからpushする。
- UI変更時は `docs/frontend-design.md` の画面の役割・共通トークン・状態別QAを基準にし、既存の `design-qa.md` の検証知見を引き継ぐ。外部ツールで本ファイルやCodex設定を上書きしない。
